# B1 round-3 fix list (judge r2: FAIL min 2 — market 4, crate 4, barrel 3, stall 3, sacks 2)
Bar: every asset ≥ 4. Market and crate are DONE — do not touch except item 4.
Give this file to the B1 builder verbatim; fresh judge after.

1. sacks — REBUILD each sack as a CLOTH form, not a lump cluster: elongated
   pillow/slug body (length > width), base flattened and spread at the ground,
   mutual squash where sacks stack, crease/fold geometry at every contact, and
   a legible gathered neck (pinched taper, thinner wrap band, floppy ear, neck
   tilted off-axis). SPEND THE BUDGET — round 2 used 1,092 of 8,000 tris and
   the starvation is why facets read as fractured rock. Smooth-shade the cloth.
   (Round-2 read: boulders/snowballs. THIRD round on this asset — if it misses
   the cloth read again, hand it to a FRESH builder.)
2. stall — MODEL the canvas attachment: visible tie wraps/lashings at both
   pole heads, IN FRAME; re-aim stall_detail at the X-crossing peg (round-2
   frame crops it); replace the raw cube pole socket with a shaped bracket.
3. barrel — re-aim barrel_detail so the bung reads in context (stave joints +
   at least one hoop in the same frame; round-2 detail was an octagonal puck
   on white); remove the stave-joint fin spikes poking above the hoop tops.
4. market — add ONE gable close-up frame so the semicircular glazed screen is
   positively evidenced (credited provisionally in r2). B1r was mid-reframe
   when the session limit hit: the last attempt showed the roof slope, not the
   gable end — camera needs to go further back and to the side for a true
   three-quarter of the gable end face.
