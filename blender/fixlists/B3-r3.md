# B3 round-3 fix list (judge r2: FAIL min 3 — pillarbox 4; rookery/viaduct/gy-flank/gaslamp 3)
Bar: every asset ≥ 4. Tiled viaduct joint PASSES — do NOT touch the arc solve.
Zero pure-black regions allowed anywhere (judge fails them as unevidenced).
Give this file to the B3 builder verbatim; fresh judge after.

1. viaduct-module — REBUILD the springing junction: close the pure-black
   TRIANGULAR HOLE where the ring meets the impost band; build the arch ring
   as a distinct proud/recessed order separate from the spandrel face (r2 shows
   one flat surface). EVIDENCE: reshoot _face as a true elevation (r2 was an
   under-deck shot) and _detail as a readable masonry close-up of the springing.
2. rookery — REBUILD the boarded window as discrete planks with gaps, nailed
   across the opening, photographed so they read (rookery_boarded.png r2 shows
   a bare recess; the GLB's "planks" material has no evidenced geometry —
   failed the inventory gate). Kill the pure-black door-moulding corner dots
   and the yard window rectangle (coincident-face class). B3r was mid-fix when
   the session limit hit: applying the picture-frame trim (from the bead
   moulding fix) to the door architrave corner squares.
3. gaslamp — REBUILD the lantern floor pass-through (black blob beside the
   burner pipe) and frame corners (black dots). ADD the vent-cap louvre gaps
   and photograph them (round-1 item, still unfixed in r2).
4. gy-flank — EVIDENCE pass: reshoot _detail with both bar ends AND socket
   caps in frame; _34 with usable exposure (r2 near white-out); _face square-on
   (settle the recess-head diagonal); investigate the mid-wall ghost hotspot.
5. pillarbox — no rebuild; optional polish (slot hood integration, serration).
