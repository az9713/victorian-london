# B1 round-5 fix list (judge r4: FAIL min 2 — stall 2, sacks 2; market 5, barrel 4, crate 4)
Bar: every asset ≥ 4. Market, barrel, crate PASS CLEAN — do not touch them.
Only two features remain in the whole batch: the stall tie and the sack-1 neck.
Give this file to the B1 builder verbatim; fresh judge after.

Both features have now failed 2+ rounds to the same cause: the builder checks
"does it read as rope/cloth?" and sees intent, not pixels. So round 5 uses
COUNTABLE properties. Render, then verify each numbered property is literally
visible and countable in the frame. If any property is not countable in the
render, the fix is not done — iterate before delivering.

1. stall tie (r4 showed: two near-identical uniform stacked rings, no tail).
   Rebuild so ALL of these are countable in stall_tie.png at the same crop:
   a. THREE or more wraps visible, radii differing by ≥15-20% of pole
      diameter (measure: loop widths in pixels must visibly differ).
   b. At least one wrap CROSSES another in depth (overlap in Z — the strand
      visibly weaves in front of/behind another wrap, not parallel stacking).
   c. A FREE TAIL extending clearly past the last wrap, long enough to droop
      under gravity (target: tail length ≥ 2x pole diameter), with the knot
      bulge at the point where the tail leaves the wraps — not merged into
      the wrap geometry.
   The curve+round-bevel primitive from r4 is the right tool — keep it; the
   failure was uniformity, not faceting. Vary radius, tilt, and center per
   wrap with real jitter.

2. sacks — sack-1 neck terminus (r4 showed: sharp angular faceted blade/beak,
   jagged crest, tapering to a point). Target silhouette: soft lumpy tied
   cloth bundle. Countable properties for sacks_detail.png and sacks_34.png:
   a. NO sharp point: the tip must be WIDER and blobbier than the pinch, not
      tapering to a point (a tied end flares after the tie).
   b. Rounded, irregular silhouette: no straight faceted edges longer than
      ~1/4 of the neck length; smooth-shade the neck.
   c. A visible TIE/CORD WRAP at the pinch point — a distinct thin band
      (different radius, reads as cord) separating body taper from the
      flared floppy end.
   Ground-contact scallop creases PASS (keep). Mutual squash was "not fully
   convincing" but not failing — if cheap, deepen sack-3's contact grooving;
   do not restructure.

Rules: zero pure-black regions; every render postdates its .blend; re-export
touched GLBs (stall.glb, sacks.glb) to sandbox/assets/models/; update
blender/manifests/B1.md tri counts honestly. Budgets: stall ≤8000 (now
5,880 — headroom exists, spend it on the tie), sacks ≤8000 (now 7,548).
