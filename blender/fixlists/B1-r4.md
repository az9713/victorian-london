# B1 round-4 fix list (judge r3: FAIL min 3 — market 3, stall 3, sacks 3; barrel 4, crate 4)
Bar: every asset ≥ 4. Barrel and crate are DONE — do not touch them.
Give this file to the B1 builder verbatim; fresh judge after.
Zero pure-black regions allowed. Every render must postdate its .blend.

1. market — the gable "screen" is currently ONE bare curved bar spanning eave
   to eave. REBUILD the gable-end glazing as an actual FAN of radiating rib
   members: multiple bars converging at the two springing points, with the
   semicircular arc as the outer chord — so it reads as "glazed semicircular
   screen with radiating iron ribs," not an unexplained bowed brace. Do not
   touch anything else on market (arcade/roof/columns verified intact).
   Re-render market_gable.png and market_face.png after.
2. stall — the pole-head tie STILL reads as mechanical clip/clamp hardware
   (two stacked rows of blocky rounded rectangular segments + a thin diagonal
   line). This is the same failure as r2 ("washers/nuts") and r3. REBUILD as
   genuinely irregular rope: individual overlapping wrap loops with visible
   gaps and overlaps, varying slightly in path, plus an asymmetric hanging
   tail with a knot bulge. Uniform stacked segments will fail again — vary
   loop radius/tilt and let loops cross. Re-render stall_tie.png at the same
   close crop. The X-crossing peg bracket PASSES — do not touch it.
   Also correct the manifest's stall tri count (it said 2,008; inventory
   says 7,056).
3. sacks — improvement held (boulder read gone, elongated bodies, pinched
   necks) but two required elements are MISSING: (a) crease/fold geometry at
   every ground-contact and sack-to-sack contact point — surfaces are
   currently smooth, not wrinkled; (b) visible MUTUAL SQUASH where sacks
   touch — they overlap but do not deform each other. Also reshape the neck
   terminus: it currently reads as a handle/spout (thin curved rod ending in
   a blob); make it an unambiguous floppy tied cloth end. Re-render all
   three sacks frames.
